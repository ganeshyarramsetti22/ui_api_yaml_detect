name: Full Stack CI/CD

on:
  push:
    branches:
      - main

  workflow_dispatch:
    inputs:
      deploy_infrastructure:
        description: "Create or update Azure infrastructure"
        required: true
        type: boolean
        default: true

permissions:
  id-token: write
  contents: read

jobs:

  # ============================================================
  # STAGE 0 - INFRASTRUCTURE
  # Runs on every push.
  # Terraform creates resources if they are not in state.
  # Existing resources managed by remote state will show no changes.
  # ============================================================
  infrastructure:
    name: Infrastructure
    runs-on: ubuntu-latest

    outputs:
      ui_app_name: ${{ steps.tf_outputs.outputs.ui_app_name }}
      api_app_name: ${{ steps.tf_outputs.outputs.api_app_name }}

    steps:

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      # --------------------------------------------------------
      # Terraform Init
      # Uses Azure Blob Storage remote state
      # --------------------------------------------------------
      - name: Terraform Init
        working-directory: terraform
        run: terraform init
        env:
          ARM_USE_OIDC: true
          ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Terraform Validate
        working-directory: terraform
        run: terraform validate

      - name: Terraform Plan
        working-directory: terraform
        run: terraform plan
        env:
          ARM_USE_OIDC: true
          ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Terraform Apply
        working-directory: terraform
        run: terraform apply -auto-approve
        env:
          ARM_USE_OIDC: true
          ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      # --------------------------------------------------------
      # Get App Service names from Terraform outputs
      # --------------------------------------------------------
      - name: Get Terraform outputs
        id: tf_outputs
        working-directory: terraform
        shell: bash
        run: |
          UI_APP_NAME=$(terraform output -raw ui_app_name)
          API_APP_NAME=$(terraform output -raw api_app_name)

          echo "ui_app_name=$UI_APP_NAME" >> "$GITHUB_OUTPUT"
          echo "api_app_name=$API_APP_NAME" >> "$GITHUB_OUTPUT"

          echo "UI App Service: $UI_APP_NAME"
          echo "API App Service: $API_APP_NAME"


  # ============================================================
  # STAGE 1 - DETECT UI / API CHANGES
  # ============================================================
  detect:
    name: Detect UI/API Changes
    needs: infrastructure
    runs-on: ubuntu-latest

    outputs:
      ui_changed: ${{ steps.filter.outputs.ui }}
      api_changed: ${{ steps.filter.outputs.api }}

    steps:

      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Detect changed components
        id: filter
        uses: dorny/paths-filter@v3
        with:
          filters: |
            ui:
              - 'ui/**'

            api:
              - 'api/**'

      - name: Show detected changes
        run: |
          echo "UI changed: ${{ steps.filter.outputs.ui }}"
          echo "API changed: ${{ steps.filter.outputs.api }}"


  # ============================================================
  # STAGE 2 - BUILD UI
  # Runs only when UI files change
  # ============================================================
  build-ui:
    name: Build UI
    needs: detect

    if: needs.detect.outputs.ui_changed == 'true'

    runs-on: ubuntu-latest

    steps:

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: ui/package-lock.json

      - name: Install UI dependencies
        working-directory: ui
        run: npm ci

      - name: Build UI
        working-directory: ui
        run: npm run build

      - name: Upload UI build
        uses: actions/upload-artifact@v4
        with:
          name: ui-build
          path: ui/dist
          retention-days: 1


  # ============================================================
  # STAGE 3 - BUILD API
  # Runs only when API files change
  # ============================================================
  build-api:
    name: Build API
    needs: detect

    if: needs.detect.outputs.api_changed == 'true'

    runs-on: ubuntu-latest

    steps:

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: api/package-lock.json

      - name: Install API dependencies
        working-directory: api
        run: npm ci

      - name: Test API
        working-directory: api
        run: npm test

      - name: Upload API build
        uses: actions/upload-artifact@v4
        with:
          name: api-build
          path: api
          retention-days: 1


  # ============================================================
  # STAGE 4 - DEPLOY UI
  # ============================================================
  deploy-ui:
    name: Deploy UI

    needs:
      - infrastructure
      - detect
      - build-ui

    if: needs.detect.outputs.ui_changed == 'true'

    runs-on: ubuntu-latest

    steps:

      - name: Download UI build
        uses: actions/download-artifact@v4
        with:
          name: ui-build
          path: ui-dist

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Deploy UI
        uses: azure/webapps-deploy@v3
        with:
          app-name: ${{ needs.infrastructure.outputs.ui_app_name }}
          package: ui-dist


  # ============================================================
  # STAGE 5 - DEPLOY API
  # ============================================================
  deploy-api:
    name: Deploy API

    needs:
      - infrastructure
      - detect
      - build-api

    if: needs.detect.outputs.api_changed == 'true'

    runs-on: ubuntu-latest

    steps:

      - name: Download API build
        uses: actions/download-artifact@v4
        with:
          name: api-build
          path: api

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Deploy API
        uses: azure/webapps-deploy@v3
        with:
          app-name: ${{ needs.infrastructure.outputs.api_app_name }}
          package: api